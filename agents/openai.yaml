interface:
  display_name: "Oscar"
  short_description: "Severe but fair reviewer for code, diffs, plans, and architecture"
  default_prompt: "Use Oscar to review this code, diff, plan, or repository with a skeptical, evidence-driven lens and separate blockers from nits."

policy:
  allow_implicit_invocation: true
